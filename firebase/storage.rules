rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Decisão da Fase 3: Storage não está ativo no MVP atual. As imagens de
    // produto serão tratadas em fase futura ou por alternativa documentada.
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
